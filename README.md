# Cafe-Ordering-System-
☕ Café Ordering System

A Python-based console application for managing food orders in a café using MongoDB as a backend database.

🔹 Key Features:

Customer information storage (Name & Contact No.)

Food category selection (Pizza / Pasta / Burger / Maggie)

Sub-menu with multiple item options

Dynamic billing with GST (18%) calculation

Order history stored in MongoDB collection

🛠 Technologies Used:

Python

MongoDB

PyMongo Library

📂 Database:

Database Name: WaradeCafe

Collection Name: orders

🎯 Purpose:

This project demonstrates basic CRUD operations using MongoDB and Python integration in a real-world café order system.
